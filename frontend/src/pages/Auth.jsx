import {useForm} from 'react-hook-form';import {api} from '../services/api';import {useAuth} from '../store/auth';import {Card} from '../components/ui/Card';
export default function Auth(){const {register,handleSubmit}=useForm();const setUser=useAuth(s=>s.setUser);const submit=async v=>{const {data}=await api.post('/auth/login',v);setUser(data.data.user)};return <Card className="mx-auto max-w-md"><h1 className="mb-4 text-3xl font-black">Login</h1><form onSubmit={handleSubmit(submit)} className="space-y-3"><input className="input" placeholder="Email" {...register('email')}/><input className="input" type="password" placeholder="Password" {...register('password')}/><button className="btn w-full">Login</button></form></Card>}
