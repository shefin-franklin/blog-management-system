import {GoogleGenerativeAI} from '@google/generative-ai';import {env} from '../config/env.js';import {AiLog} from '../models/misc.js';
const model=()=>env.geminiApiKey?new GoogleGenerativeAI(env.geminiApiKey).getGenerativeModel({model:'gemini-1.5-flash'}):null;
export async function generateAI({feature,prompt,user}){const m=model();let text;if(m){const r=await m.generateContent(prompt);text=r.response.text();}else{text=localAI(feature,prompt);}await AiLog.create({user,feature,prompt,response:text});return text;}
function localAI(feature,prompt){const p=prompt.slice(0,140);return JSON.stringify({title:`${feature} for ${p}`.slice(0,70),summary:`AI-ready response generated from configured prompt: ${p}`,tags:['mern','blog','ai','seo'],metaDescription:`Optimized content about ${p}`.slice(0,155),content:`# ${feature}\n\n${p}\n\nThis draft is structured for clarity, search visibility, and reader engagement.`});}
export async function moderateComment(content,user){const raw=await generateAI({feature:'moderation',prompt:`Return JSON toxicity and spam scores for: ${content}`,user});try{return JSON.parse(raw);}catch{return {toxicity:0.05,spam:/https?:\/\//i.test(content)?0.7:0.05,reason:'Rule-assisted moderation'}}}
